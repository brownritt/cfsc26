# Interfaces activity

Demonstrated some aspects of working in an IDE.

A key idea is the distinction between the interface itself (e.g. VS Code's Jupyter notebook UI) and the underlying kernels that do the actual computation.

Look at `code.ipynb` and `helper_code.py` for details.
